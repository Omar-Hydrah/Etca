server:
  port: 80

spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/etca
    username: root
    password: 1234

logging:
  level:
    org:
      # springframework:
        # web: ERROR
        # security: ERROR
      hibernate: ERROR